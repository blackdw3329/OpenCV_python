```python
import numpy as np, cv2
image = cv2.imread("lenna.bmp", cv2.IMREAD_GRAYSCALE)
if image is None: raise Exception("영상파일 읽기 오류")

noise = random_matrix = np.random.randint(0, 50, image.shape[:2], dtype=np.uint8)

dst = cv2.add(image,noise)

kernel1 = np.ones((9, 9), np.float32) / 81
blur1 = cv2.filter2D(dst, -1, kernel1)
cv2.imshow("image", image)
cv2.imshow("noise",dst)
cv2.imshow("blur", blur1)
cv2.waitKey(0)
```
## 결과화면

<img src="">
